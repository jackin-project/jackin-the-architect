# SPDX-FileCopyrightText: 2026 Alexey Zhokhov
# SPDX-License-Identifier: Apache-2.0

name: Publish Image

concurrency:
  # Serialize publishes per branch so simultaneous merges (for example
  # several Renovate PRs landing within seconds) do not race on the
  # :latest tag and leave it pointing at an older commit's image. The
  # newest push wins, keeping :latest current so launches can reuse the
  # published base instead of rebuilding from the workspace Dockerfile.
  group: publish-image-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      lane:
        description: |
          Runner lane. Publish mutates registry state, so it runs on one
          lane: velnor (default), github, or both. In both mode Velnor is
          the sole publisher and GitHub performs a read-only parity build.
        type: choice
        default: velnor
        options: [velnor, github, both]

jobs:
  publish-velnor:
    if: github.event_name != 'workflow_dispatch' || inputs.lane == 'velnor' || inputs.lane == 'both'
    uses: jackin-project/jackin-role-action/.github/workflows/publish.yml@8b8da8b8efe76474559c0abdb4f833eef3f8a166 # pinned
    permissions:
      contents: read
      id-token: write
    with:
      # Use native arm64 runners on the github lane. When both architectures
      # use the same runner label, the reusable workflow builds arm64 via QEMU.
      runner-amd64: velnor-target-mvp
      runner-arm64: velnor-target-mvp
      runner-merge: velnor-target-mvp
      publish: true
    secrets:
      registry-username: ${{ secrets.DOCKERHUB_USERNAME }}
      registry-password: ${{ secrets.DOCKERHUB_TOKEN }}
      github-readonly-token: ${{ secrets.GH_READONLY_TOKEN }}

  publish-github:
    if: github.event_name == 'workflow_dispatch' && (inputs.lane == 'github' || inputs.lane == 'both')
    uses: jackin-project/jackin-role-action/.github/workflows/publish.yml@8b8da8b8efe76474559c0abdb4f833eef3f8a166 # pinned
    permissions:
      contents: read
      id-token: write
    with:
      runner-amd64: ubuntu-26.04
      runner-arm64: ubuntu-26.04-arm
      runner-merge: ubuntu-26.04
      publish: ${{ inputs.lane == 'github' }}
    secrets:
      registry-username: ${{ secrets.DOCKERHUB_USERNAME }}
      registry-password: ${{ secrets.DOCKERHUB_TOKEN }}
      github-readonly-token: ${{ secrets.GH_READONLY_TOKEN }}
